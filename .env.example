# Google OAuth2 設定
# 從 Google Cloud Console 取得: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your_client_id_here
GOOGLE_CLIENT_SECRET=your_client_secret_here
GOOGLE_REDIRECT_URI=http://localhost:3000/oauth2callback

# Google Sheets 設定
# Google Sheets 的 ID（從 URL 中取得）
# 例如: https://docs.google.com/spreadsheets/d/SPREADSHEET_ID/edit
SPREADSHEET_ID=your_spreadsheet_id_here

# 工作表名稱（預設為「訂單」）
SHEET_NAME=訂單

# Gmail 搜尋設定
# Cyberbiz 寄件者 email（用來過濾訂單郵件）
CYBERBIZ_SENDER=noreply@cyberbiz.co

# 排程設定（cron 格式，預設每 10 分鐘執行一次）
CRON_SCHEDULE=*/10 * * * *

# 首次抓取的天數範圍（預設 30 天）
INITIAL_FETCH_DAYS=30
